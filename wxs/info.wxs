
// js代码(es5)
var message = "hello world";
var name = "coderRedw";

var sum = function (num1, num2) {
  return num1 + num2
}
module.exports = {
  message: message,
  name: name,
  sum: sum
}