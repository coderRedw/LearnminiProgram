<!--page/wxs/wxs.wxml-->

<!-- 1.错误的写法 -->
<!-- <view>{{ 252.2352352.tofixed(2) }}</view> -->


<!-- wxs的基本使用 -->
<!-- <wxs module="info">
  // js代码(es5)
  var message = "hello world"
  var name = "coderRedw"

  var sum = function (num1, num2)　{
    return num1 + num2
  }
  module.exports = {
    message: message,
    name: name,
    sum: sum
  }
</wxs> -->

<!-- 定义在单独的wxs文件中，再使用<wxs>标签进入导入 -->
<!-- 不能使用绝对路径，必须使用相对路径 -->
<wxs src="../../wxs/info.wxs" module="info"/>

<view>{{ info.message }}</view>
<view>{{ info.name }}</view>
<view>{{ info.sum(20, 30) }}</view>

<!-- wxs的应用 -->
<wsx src="../../wxs/format.wxs" module="format"/>
<view>{{ format.priceFormat(price, 3) }}</view>

<view>{{ format.dateFormat(timer, 'yyy/mm-dd')}}</view>
